# Exact versions installed on the hosting platform.
#
# These are the versions the test suite is run against locally, pinned so that a
# redeploy months from now builds the same thing. requirements.txt keeps the
# wider ranges that development uses.
#
# Every pin here has a ready-built wheel for the Python version named in
# .python-version (3.13). If you raise that file, check these still do - the
# alternative is the build trying to compile them from source and failing.
Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.1.0
Werkzeug==3.1.8
SQLAlchemy==2.0.52
alembic==1.19.2
gunicorn==23.0.0
# The PostgreSQL driver. 2.9.12 is the first release with wheels for Python
# 3.13 and 3.14; 2.9.9 has none and would try to compile from source.
psycopg2-binary==2.9.12
pandas==3.0.5
openpyxl==3.1.5
pypdf==5.9.0
reportlab==4.5.1
google-api-python-client==2.200.0
google-auth==2.57.1
anthropic==0.125.0
python-dotenv==1.2.3
