import { ComingSoon } from '@/app/Pages'

/** Placeholder until the Work Orders screens land (see docs/plans). */
export default function WorkOrdersPage() {
  return <ComingSoon title="Work Orders" stage={2} description="Work-order list, filters, saved views, creation pane and detail." />
}
