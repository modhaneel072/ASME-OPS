import { ComingSoon } from '@/app/Pages'

/** Placeholder until the Requests screens land (see docs/plans). */
export default function RequestsPage() {
  return <ComingSoon title="Requests" stage={3} description="Request submission, review, clarification, approval and conversion to work orders." />
}
