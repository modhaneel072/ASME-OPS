import { ComingSoon } from '@/app/Pages'

/** Placeholder until the Projects screens land (see docs/plans). */
export default function ProjectsPage() {
  return <ComingSoon title="Projects" stage={2} description="Project list, overview, milestones, teams, assets, documents, budget and activity." />
}
