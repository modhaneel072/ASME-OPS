import { ComingSoon } from '@/app/Pages'

/** Placeholder until the Settings screens land (see docs/plans). */
export default function SettingsPage() {
  return <ComingSoon title="Settings" stage={1} description="Profile, chapter, roles, notifications, integrations and audit settings." />
}
