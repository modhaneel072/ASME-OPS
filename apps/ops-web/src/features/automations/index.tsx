import { ComingSoon } from '@/app/Pages'

/** Placeholder until the Automations screens land (see docs/plans). */
export default function AutomationsPage() {
  return <ComingSoon title="Automations" description="Triggers, conditions, actions and execution history." />
}
