import { ComingSoon } from '@/app/Pages'

/** Placeholder until the Sponsors screens land (see docs/plans). */
export default function SponsorsPage() {
  return <ComingSoon title="Sponsors" description="Sponsor records, tiers and deliverables." />
}
