import { ComingSoon } from '@/app/Pages'

/** Placeholder until the Parts Inventory screens land (see docs/plans). */
export default function PartsPage() {
  return <ComingSoon title="Parts Inventory" stage={4} description="Parts, storage locations and immutable inventory transactions." />
}
