import { ComingSoon } from '@/app/Pages'

/** Placeholder until the Notifications screens land (see docs/plans). */
export default function NotificationsPage() {
  return <ComingSoon title="Notifications" stage={2} description="In-app notification center." />
}
