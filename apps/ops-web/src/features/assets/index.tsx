import { ComingSoon } from '@/app/Pages'

/** Placeholder until the Assets screens land (see docs/plans). */
export default function AssetsPage() {
  return <ComingSoon title="Assets" stage={2} description="Asset hierarchy, status history and work-order links." />
}
