import { ComingSoon } from '@/app/Pages'

/** Placeholder until the Setup Center screens land (see docs/plans). */
export default function SetupPage() {
  return <ComingSoon title="Setup Center" stage={1} description="Guided chapter setup with derived progress." />
}
