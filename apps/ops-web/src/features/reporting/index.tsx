import { ComingSoon } from '@/app/Pages'

/** Placeholder until the Reporting screens land (see docs/plans). */
export default function ReportingPage() {
  return <ComingSoon title="Reporting" stage={2} description="Operations dashboard, project and asset health, drill-down reporting and exports." />
}
