import { ComingSoon } from '@/app/Pages'

/** Placeholder until the Categories screens land (see docs/plans). */
export default function CategoriesPage() {
  return <ComingSoon title="Categories" stage={1} description="Category list and detail with usage counts." />
}
