import { ComingSoon } from '@/app/Pages'

/** Placeholder until the Teams / Users screens land (see docs/plans). */
export default function TeamsUsersPage() {
  return <ComingSoon title="Teams / Users" stage={1} description="Teams, member directory, invitations and roles." />
}
