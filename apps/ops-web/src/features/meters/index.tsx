import { ComingSoon } from '@/app/Pages'

/** Placeholder until the Meters screens land (see docs/plans). */
export default function MetersPage() {
  return <ComingSoon title="Meters" stage={6} description="Manual and API readings with threshold triggers." />
}
