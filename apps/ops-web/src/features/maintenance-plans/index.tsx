import { ComingSoon } from '@/app/Pages'

/** Placeholder until the Maintenance Plans screens land (see docs/plans). */
export default function MaintenancePlansPage() {
  return <ComingSoon title="Maintenance Plans" description="Recurring plans that generate work orders exactly once." />
}
