import { ComingSoon } from '@/app/Pages'

/** Placeholder until the Vendors screens land (see docs/plans). */
export default function VendorsPage() {
  return <ComingSoon title="Vendors" stage={2} description="Vendor directory used by work orders and purchasing." />
}
