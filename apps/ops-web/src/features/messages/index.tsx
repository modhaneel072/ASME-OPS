import { ComingSoon } from '@/app/Pages'

/** Placeholder until the Messages screens land (see docs/plans). */
export default function MessagesPage() {
  return <ComingSoon title="Messages" stage={3} description="Team, project, group and direct conversations with threads and reactions." />
}
