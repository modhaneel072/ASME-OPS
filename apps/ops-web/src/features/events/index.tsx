import { ComingSoon } from '@/app/Pages'

/** Placeholder until the Events screens land (see docs/plans). */
export default function EventsPage() {
  return <ComingSoon title="Events" description="Chapter events with checklists, attendance and generated work orders." />
}
